# render.yaml - Render deployment configuration
# Generated by QuantumLayer Factory

services:
  - type: web
    name: general-api
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    dockerContext: .
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8080
      - key: NODE_ENV
        value: production
    healthCheckPath: /health

  - type: postgres
    name: general-api-db
    plan: starter
    databaseName: general-api
    databaseUser: general-api
